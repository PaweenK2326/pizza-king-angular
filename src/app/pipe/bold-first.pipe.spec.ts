import { BoldFirstPipe } from './bold-first.pipe';

describe('BoldFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
