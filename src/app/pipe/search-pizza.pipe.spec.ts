import { SearchPizzaPipe } from './search-pizza.pipe';

describe('SearchPizzaPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPizzaPipe();
    expect(pipe).toBeTruthy();
  });
});
